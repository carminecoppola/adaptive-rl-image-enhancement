dataset:
  name: "CIFAR10"
  image_size: 32
  train_split: true
  download: true

degradation:
  type: "mixed"
  candidate_types:
    - "gaussian_noise"
    - "low_brightness"
    - "low_contrast"
    - "blur"
    - "combined"
  noise_std: 0.1
  low_contrast: false
  low_brightness: false
